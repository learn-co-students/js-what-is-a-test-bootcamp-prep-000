// write your code below!
// made changes to run tests to complete lesson
var name = "Susan"
var height = 38
var message = `${name} is ${height} inches tall`

module.exports = {name, height, message}

// log the message to console
console.log(message)