// write your code below!
var name = "Joe"
var height = 74
var message = `${name} is ${height} inches tall`
//in order to grab the variable with the ${} you need to use back ticks `` instead of the standard "quotes"
//this is so you don't have to hard code anything that doesn't need to be hardcoded

module.exports = {name, height, message}
