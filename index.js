// write your code below!
var name = "Susan"
var height = 39
var message = `${name} is ${height} inches tall`
// other possibility : var message2 = name + ' is ' + height + ' inches tall'

console.log(message)

module.exports = {name, height, message}
