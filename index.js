// write your code below!
var name = "Joe"
var height = 0
var message = `${name} is ${height} inches tall because he doesnt exist`

module.exports = {name, height, message}
console.log(message)

var message = `${name} is ${height} inches tall`;
module.exports = {name, height, message};
console.log(message);
