// write your code below!
var name = "Joe"
var height = 74
var message = `${name} is ${height} inches tall`
//If you create a string wrapping it in back-ticks (`) you can then use your variables directly in the string to create a sentence like we just did.
//Format to insert variables:  ${ var name }
module.exports = {name, height, message}

console.log(message);
